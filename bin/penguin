# require 'penguin'
# this doesnt have to require everything before it runs

# What this binary will do :
#   --help
#   --rails : bootstraps a rails app with dummy config and rake tasks possibly adds to config.gem
#   --no-daemonize defaults to daemonizing
#   --config= defaults to ./config/daemons.rb
#   --pid-directory defaults to ./pids
#   gui
#   status
#   --client ? starts a preloaded irb sessions?

puts "I don't do anything yet"
